# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit keys
.env
.env.*
!.env.example

# Figma asset masters — full-resolution PNG/JPEG pulled out of source design
# files for a case study. Only the converted, resized WebP in src/assets/ ships;
# the masters stay local so multi-MB renders can't ride along in a commit.
zeat-assets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.claude/*
!.claude/launch.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
moodmuse-assets
