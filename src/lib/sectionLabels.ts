// Single source of truth for the "Workshop" section label — shared by the
// homepage section heading, its nav links (desktop + mobile), the Studio
// Waters project's listSection eyebrow (shown on its own detail page), and
// the DotGrid hero-canvas orb subtitles, so none of them can drift out of
// sync with each other.
export const WORKSHOP_SECTION_LABEL = "Workshop";
